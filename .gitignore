# deps + the single-binary build output
node_modules/
/unforge
/unforge.exe
.dts-review/

# secrets — credentials, private keys, and the sealed account store
.env*
!.env.example
*.p12
*.pfx
*.pem
accounts.dat*

# …except the launcher's cert, which is bundled (src/core/cert.ts).
!src/core/gameforge-cert.pem

# captured launcher traffic + request traces — hold passwords, tokens, blackboxes
scripts/captures/
*.jsonl
