// Metin2's own identifiers (pt environment), from a captured creation request. Both the Spark
// endpoints and the client handoff name the game, so the values live above either of them.

/** The game. Spark spells it `gameId`; the client handoff spells it `_TNT_CLIENT_APPLICATION_ID`. */
export const METIN2_GAME_ID = "fab180a3-cd65-4b7e-bd0e-2ef77fd0c258";

export const METIN2_GAME_ENVIRONMENT_ID = "5401ee5b-1316-41ae-a628-73377b8676ba";
